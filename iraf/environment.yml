name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - jupyterlab
  - numpy
  - astropy
  - matplotlib
  - scipy
  - pip
  - pip:
      # jupyter-desktop-server não está no conda-forge; sucessor mantido pela JupyterHub
      - jupyter-remote-desktop-proxy
      - pyraf
