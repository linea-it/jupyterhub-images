name: base
channels:
  - conda-forge
  - defaults
dependencies:
  # --- Database Access & Querying ---
  - psycopg2
  - sqlalchemy
  # --- Pip-only packages ---
  - pip
  - pip:
    - lineassp
    - pzserver
    - sora-astro
